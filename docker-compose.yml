version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: scholarship
    restart: unless-stopped
    ports:
      - "80:3000"
    environment:
      - PORT=3000

